query getAssetClass($underlyingInstrumentGroupId: Int!){
  group(id: $underlyingInstrumentGroupId) {
    id
    derivativeAssetClassBucket {
      assetClass {
        id
        name

      }
      count
    }
  }
}